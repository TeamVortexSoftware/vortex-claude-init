{
  "name": "@teamvortexsoftware/vortex-claude",
  "version": "0.1.6",
  "description": "Add Vortex AI integration to Claude Code in your project",
  "bin": {
    "vortex-claude": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "echo 'This package should not be published. Use publish:stg or publish:prod' && exit 1",
    "build:stg": "pnpm run distclean && pnpm run build && mkdir -p dist.d/stg && mv dist dist.d/stg && echo && echo 'Build in ./dist.d/stg'",
    "prepublish:stg": "cp ./LICENSE ./README.md ./dist.d/stg/ && jq '.name = \"@teamvortexsoftware/vortex-claude-stg\" | .description = \"Add Vortex AI integration to Claude Code in your project (STG)\" | del(.scripts.prepack)' ./package.json > ./dist.d/stg/package.json",
    "publish:stg": "pnpm run prepublish:stg && pnpm publish --access restricted ./dist.d/stg",
    "build:prod": "pnpm run distclean && pnpm run build && mkdir -p dist.d/prod && mv dist dist.d/prod && echo && echo 'Build in ./dist.d/prod'",
    "prepublish:prod": "cp ./LICENSE ./README.md ./dist.d/prod/ && jq 'del(.scripts.prepack)' ./package.json > ./dist.d/prod/package.json",
    "publish:prod": "pnpm run prepublish:prod && pnpm publish --access public ./dist.d/prod",
    "check-types": "tsc --noEmit",
    "distclean": "rm -rf ./dist ./dist.d",
    "clean": "rm -rf ./dist",
    "build": "tsc && pnpm run copy-templates",
    "copy-templates": "mkdir -p dist/templates/vortex/fallbacks/backend dist/templates/vortex/fallbacks/frontend && cp ../../.claude/commands/integrate-vortex.md dist/templates/ && cp ../../.claude/commands/vortex/discover.md dist/templates/vortex/ && cp ../../.claude/commands/vortex/fallbacks/backend/*.md dist/templates/vortex/fallbacks/backend/ && cp ../../.claude/commands/vortex/fallbacks/frontend/*.md dist/templates/vortex/fallbacks/frontend/"
  },
  "keywords": [
    "vortex",
    "claude",
    "ai",
    "integration",
    "invitations"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "catalog:"
  }
}
